query getPaymentSuccessData($sessionID: UUID!) {
	getSessionByID(sessionID: $sessionID) {
		sessionID
		title
		price
		equipmentFee
		startTime
		endTime
		capacityRemaining
		isCancelled
		location {
			locationID
			name
		}
		course {
			courseID
			name
			photo
		}
	}
}
